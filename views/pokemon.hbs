{{#each pokemon}}
    <div class="box">
        <h3>{{name}}</h3>
        <img src="{{image}}" alt="name" class="pokemon-img">
    </div>
{{/each}}

{{!-- make first letter of name capital
get the order right --}}